charm_name: ceph-iscsi

gate_bundles:
  - jammy-zed

smoke_bundles:
  - jammy-zed

dev_bundles:
  - jammy-zed

configure:
  - tests.target.basic_guest_setup
tests:
  - tests.target.CephISCSIGatewayTest

